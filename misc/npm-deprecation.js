throw new Error("You need to install this package FROM the jsr registry. The NPM package is just a placehodler\n\nnpx jsr add @cupglassdev/duckncup\n\nAfter the installation, NEVER actually delete the .npmrc file, because it is linked to your package.json")